apiVersion: v1
kind: Service
metadata:
  name: contentflow-frontend
  labels:
    app.kubernetes.io/name: contentflow
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: contentflow
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
