apiVersion: v1
kind: Service
metadata:
  name: contentflow-backend
  labels:
    app.kubernetes.io/name: contentflow
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: contentflow
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
